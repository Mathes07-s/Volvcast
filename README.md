# Volvcast
Volvcast – Cloud‑relayed, ultra‑low‑latency classroom audio. FastAPI + WebSockets server on Render relays PCM from a local Python broadcaster to 50+ listeners. Listener UI uses a strict jitter buffer and wakeLock for stable, near‑real‑time playback.
